name: Retry pending sermon requests

on:
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "Success-mode version does not use automatic YouTube transcription retry."
          echo "Create or edit an issue with a sermon transcript, then run Process sermon archive request."
